(ns clojure-course.data-and-abstraction.ex-05-rational-arithmetic)

(defn make-rat
  "Construct a rational number n/d, reduced to lowest terms.
   Denominator is always positive."
  [n d]
  (throw (ex-info "not implemented" {})))

(defn numer
  "Return the numerator of a rational."
  [r]
  (throw (ex-info "not implemented" {})))

(defn denom
  "Return the denominator of a rational."
  [r]
  (throw (ex-info "not implemented" {})))

(defn add-rat
  "Add two rationals, returning a new rational in lowest terms."
  [r1 r2]
  (throw (ex-info "not implemented" {})))

(defn mul-rat
  "Multiply two rationals, returning a new rational in lowest terms."
  [r1 r2]
  (throw (ex-info "not implemented" {})))

(defn equal-rat?
  "Return true if r1 and r2 represent the same rational value."
  [r1 r2]
  (throw (ex-info "not implemented" {})))
