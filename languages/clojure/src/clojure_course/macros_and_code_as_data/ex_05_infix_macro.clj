(ns clojure-course.macros-and-code-as-data.ex-05-infix-macro)

;; TASK: Implement `infix` so that (infix 2 + 3) expands to (+ 2 3).
;; Use `list` to construct the output form.

(defmacro infix [left op right]
  nil)
