(ns clojure-course.recursion-and-recur.ex-07-tree-sum)

(defn tree-sum
  "Sum all numeric leaves in a nested vector structure.
   A leaf is number?; a branch is sequential?."
  [tree]
  (throw (ex-info "not implemented" {})))
