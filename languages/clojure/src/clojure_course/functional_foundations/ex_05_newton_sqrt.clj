(ns clojure-course.functional-foundations.ex-05-newton-sqrt)

(defn good-enough?
  "Returns true if guess squared is within 0.00001 of n."
  [guess n]
  (throw (ex-info "not implemented" {})))

(defn improve
  "Returns an improved guess: (guess + n/guess) / 2."
  [guess n]
  (throw (ex-info "not implemented" {})))

(defn sqrt
  "Compute the square root of n using Newton's method."
  [n]
  (throw (ex-info "not implemented" {})))
