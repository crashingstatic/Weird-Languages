(ns clojure-course.higher-order-seqs.ex-10-running-stats)

;; Implement running-stats: given a sequence of numbers, return a sequence
;; of {:count n :sum s :mean m} maps showing the running statistics.

(defn running-stats
  [numbers]
  (throw (ex-info "not implemented" {})))
