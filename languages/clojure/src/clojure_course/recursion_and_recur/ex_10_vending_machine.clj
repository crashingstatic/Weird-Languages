(ns clojure-course.recursion-and-recur.ex-10-vending-machine)

;; Implement run-machine: process a sequence of commands against a
;; vending machine state and return the final state.
;; See PROBLEM.md for the command format and state shape.

(defn run-machine
  [state commands]
  (throw (ex-info "not implemented" {})))
