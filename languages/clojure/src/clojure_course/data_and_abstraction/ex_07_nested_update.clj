(ns clojure-course.data-and-abstraction.ex-07-nested-update)

(defn get-stat
  "Return the value of stat for player on team."
  [data team player stat]
  (throw (ex-info "not implemented" {})))

(defn inc-stat
  "Return data with the given stat incremented by 1."
  [data team player stat]
  (throw (ex-info "not implemented" {})))

(defn add-player
  "Return data with a new player added to team with {:goals 0 :assists 0}."
  [data team player]
  (throw (ex-info "not implemented" {})))
