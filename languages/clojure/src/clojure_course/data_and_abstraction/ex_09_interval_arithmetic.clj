(ns clojure-course.data-and-abstraction.ex-09-interval-arithmetic)

;; Implement interval arithmetic: make-interval, lower-bound, upper-bound,
;; add-interval, mul-interval, width, contains-zero?.

(defn make-interval [lo hi]
  (throw (ex-info "not implemented" {})))

(defn lower-bound [i]
  (throw (ex-info "not implemented" {})))

(defn upper-bound [i]
  (throw (ex-info "not implemented" {})))

(defn add-interval [a b]
  (throw (ex-info "not implemented" {})))

(defn mul-interval [a b]
  (throw (ex-info "not implemented" {})))

(defn width [i]
  (throw (ex-info "not implemented" {})))

(defn contains-zero? [i]
  (throw (ex-info "not implemented" {})))
