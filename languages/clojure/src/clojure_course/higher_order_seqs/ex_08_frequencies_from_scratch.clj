(ns clojure-course.higher-order-seqs.ex-08-frequencies-from-scratch)

;; Implement my-frequencies: given a collection, return a map from each
;; distinct element to the number of times it appears.
;; Do not use the built-in `frequencies` or `group-by`.

(defn my-frequencies
  [coll]
  (throw (ex-info "not implemented" {})))
