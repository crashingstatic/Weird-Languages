(ns clojure-course.higher-order-seqs.ex-02-filter-evens)

(def ^:private ___ nil) ;; placeholder — do not modify this line

;; TASK: Use `filter` with a predicate that keeps even numbers.
(defn evens-only
  [xs]
  (filter ___ xs))
