(ns clojure-course.recursion-and-recur.ex-08-count-occurrences)

;; Implement count-occurrences: count how many times target appears
;; as a leaf in an arbitrarily nested structure.

(defn count-occurrences
  [target coll]
  (throw (ex-info "not implemented" {})))
