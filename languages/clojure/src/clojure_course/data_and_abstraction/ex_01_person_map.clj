(ns clojure-course.data-and-abstraction.ex-01-person-map)

(def ^:private ___ nil)

;; TASK: Define `person` as a map with :name, :age, and :email.
(def person ___)

;; TASK: Define `greet` — given a person map, return "Hello, <name>!"
(defn greet [p]
  ___)
