(ns clojure-course.higher-order-seqs.ex-09-compose-many)

;; Implement compose-all: given a sequence of single-argument functions,
;; return a function that is their right-to-left composition.
;; Do not use the built-in `comp`.

(defn compose-all
  [fs]
  (throw (ex-info "not implemented" {})))
