(ns clojure-course.macros-and-code-as-data.ex-02-unless-macro)

;; TASK: Fill in the body of `unless`.
;; (unless condition then else) evaluates `then` when condition is FALSE
;; and `else` when condition is TRUE — the opposite of `if`.

(def ^:private ___ nil)

(defmacro unless [condition then-expr else-expr]
  ___)
