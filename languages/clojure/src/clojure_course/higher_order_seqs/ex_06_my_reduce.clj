(ns clojure-course.higher-order-seqs.ex-06-my-reduce)

(defn my-reduce
  "Reduces coll with f. Three-arity uses init; two-arity uses first element."
  ([f init coll]
   (throw (ex-info "not implemented" {})))
  ([f coll]
   (throw (ex-info "not implemented" {}))))
