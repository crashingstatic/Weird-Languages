(ns clojure-course.functional-foundations.ex-09-fib-two-ways)

;; Implement fib-tree and fib-iter.
;; See PROBLEM.md for the algorithm descriptions.

(defn fib-tree
  [n]
  (throw (ex-info "not implemented" {})))

(defn fib-iter
  [n]
  (throw (ex-info "not implemented" {})))
