(ns clojure-course.data-and-abstraction.ex-08-binary-tree-of-data)

;; Implement a BST as nested maps {:value v :left l :right r}.
;; nil represents an empty tree.

(defn bst-insert
  [tree val]
  (throw (ex-info "not implemented" {})))

(defn bst-contains?
  [tree val]
  (throw (ex-info "not implemented" {})))

(defn bst-in-order
  [tree]
  (throw (ex-info "not implemented" {})))
