(ns clojure-course.higher-order-seqs.ex-05-my-filter)

(defn my-filter
  "Returns a sequence of elements from coll for which pred returns truthy."
  [pred coll]
  (throw (ex-info "not implemented" {})))
